node_modules/
build/
dist
bin/
coverage/

.cache
.yarn/cache
.eslintcache

.cursor

.DS_Store
.env
logs_*/
*.log
.jules/

# AI Agent
JULES.md

# Code review knowledge graph
.code-review-graph/

# Generated files
*_e2e_result.json
docs/superpowers/

# AI Assistant / Rules
.jules/
.jules
.superpower/
superpower/
superpowers/
docs/superpowers/

# Redundant lockfile (bun.lock is authoritative) — 2026-06-07
package-lock.json

# Bot/merge junk artifacts (prevent accidental commits) — 2026-06-07
*.orig
*.rej
*.cover
*.bak
/*.patch
/*.diff

# cf_full_flow.py per-run OAuth state (PKCE verifier + bearer JWT) — never commit
scripts/.notion_cf_token*

# Deploy-time wrangler config with REAL account/KV IDs (cf:deploy, deploy_cf.py)
wrangler.deploy.jsonc

# npm pack output (`bun run build:pack` or `npm pack`) — local artifact, never commit
*.tgz
